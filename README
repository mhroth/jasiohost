JAsioHost is a Java ASIO host, meant to reduce input and output audio 
latency in Java (considering that the Java Sound API is quite slow). 
There is nothing to see here yet, but once basic functionality has been 
reasonably implemented, a first commit will be submitted.
